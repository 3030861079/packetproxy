name: Build APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            python3-pip python3-dev build-essential \
            git zip unzip openjdk-17-jdk \
            autoconf libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo5 \
            cmake libffi-dev libssl-dev binutils libltdl-dev
          python3 -m pip install --upgrade pip -q
          python3 -m pip install buildozer cython -q

      - name: Build APK (pass 1 - build hostpython, kivy may fail)
        continue-on-error: true
        run: yes | buildozer -v android debug || true

      - name: Patch hostpython cgi module
        run: bash patches/patch_cgi.sh

      - name: Build APK (pass 2)
        run: buildozer -v android debug

      - name: Upload APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: PacketProxy-APK
          path: bin/*.apk
          retention-days: 30
